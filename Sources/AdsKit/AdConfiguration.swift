//
//  AdConfiguration.swift
//
//  Removed in AdsKit 2.0. The fixed 13-slot `AdConfiguration` + `AdMobId` +
//  `AdTypes` model was replaced by the app-defined `AdSlot` protocol (see
//  AdSlot.swift), which lets each app declare any number of placements per
//  format. Apps on the 1.0.x line still use the old model via that tag.
//
//  This file is intentionally empty; safe to delete from the package.
//
